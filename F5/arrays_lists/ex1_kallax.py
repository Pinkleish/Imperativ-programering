# A Sequence is an *ordered* collection
# of elements.

# We can *initialize* an array like this:
min_kallax = ['penna',
              'Think Python',
              'bärbar dator',
              'anteckningsblock']

print(min_kallax)
