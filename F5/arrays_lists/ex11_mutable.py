# Lists are *mutable*.
# We can change values to other values.

min_kallax = ['penna',
              'Think Python',
              'bärbar dator',
              'anteckningsblock']


# We use the same syntax
min_kallax[0] = 'äpple'

print(min_kallax)