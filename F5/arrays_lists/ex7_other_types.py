# We can use any other type, integers...
kallax1 = [3, 6, 8, -5]

# Don't confuse *indices* with *values*.
print(kallax1[2])

# ...booleans...
kallax2 = [True, False, True]

# ...and floats...
kallax3 = [4.5, 6.7, 1.0]

# We avoid mixing types for this course.
# (although it is allowed in Python)