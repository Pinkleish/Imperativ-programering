min_kallax = ['penna',
              'Think Python',
              'bärbar dator',
              'anteckningsblock']

# len(..) is useful for getting the *last*
# element:

kallax_length = len(min_kallax)

print(
    min_kallax[kallax_length - 1]
      )