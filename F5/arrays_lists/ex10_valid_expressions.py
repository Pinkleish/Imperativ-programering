# Of course, we can use expressions in all the usual
# ways:

# as function arguments/parameters:
a = ['Pelle', 'Maja', 'Trisse', 'Gullan']
print(len(a[1]))

# with the operators:
my_variable_six = 6
b = [5, my_variable_six , 7]
print(b[1] + b[2])

i = 2
print(b[i] % 2 == 0)