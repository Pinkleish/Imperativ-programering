some_numbers = [3, 6, 8, -1]

# The sorted function returns a new list.
# it works for lists.
sorted_numbers = sorted(some_numbers)

print(sorted_numbers)

# The original list is unchanged
print(some_numbers)